<div class="contact-partial">
    <div class="left-side">
        <h1 class="main-text">{{heroText}}</h1>
        <a class="contact-button general-button" href="/contact">Contact Me</a>
    </div>
    {{> contactCard}}
</div>